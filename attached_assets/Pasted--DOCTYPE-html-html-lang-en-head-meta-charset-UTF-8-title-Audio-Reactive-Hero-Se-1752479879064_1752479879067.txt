<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Audio Reactive Hero Section</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- dat.gui and three.js are loaded via ES modules in the script -->
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
      overflow: hidden;
    }
    body {
      background: #111;
      min-height: 100vh;
      font-family: 'Inter', sans-serif;
    }
    #container {
      position: absolute;
      top: 0; left: 0; width: 100vw; height: 100vh;
      z-index: 1;
    }
    .hero-content {
      position: absolute;
      top: 30%;
      left: 50%;
      transform: translate(-50%, -30%);
      z-index: 2;
      text-align: center;
      color: #fff;
      width: 100vw;
      pointer-events: none;
      user-select: none;
    }
    .hero-content h1 {
      font-size: 3rem;
      font-weight: 700;
      margin: 0 0 1rem 0;
      letter-spacing: 1px;
      text-shadow: 0 2px 12px #0009;
    }
    .hero-content button {
      pointer-events: auto;
      font-size: 1.15rem;
      padding: 0.75em 2.5em;
      border: none;
      border-radius: 2em;
      background: rgba(255,200,0,0.85);
      color: #222;
      font-weight: 700;
      cursor: pointer;
      transition: background .18s;
      box-shadow: 0 2px 16px #0003;
      outline: none;
    }
    .hero-content button:hover {
      background: #fff200;
    }
    #fps {
      position: fixed;
      top: 10px;
      right: 14px;
      color: #fff;
      background: #000a;
      padding: 4px 10px;
      border-radius: 6px;
      font-size: 13px;
      font-family: monospace;
      z-index: 1100;
      pointer-events: none;
    }
    .custom-cursor {
      position: fixed;
      top: 0; left: 0;
      width: 18px; height: 18px;
      border: 2.5px solid #fff8;
      border-radius: 50%;
      pointer-events: none;
      mix-blend-mode: difference;
      background: #fff2;
      z-index: 1200;
      transform: translate(-50%, -50%);
      transition: background .15s, border .2s;
    }
    @media (max-width: 520px) {
      .hero-content h1 { font-size: 1.6rem; }
      .hero-content { top: 16%; }
      .hero-content button { font-size: 1rem; padding: 0.65em 1.8em; }
    }
  </style>
</head>
<body>
  <div id="container"></div>
  <div class="hero-content">
    <h1>Audio Reactive Hero</h1>
    <button id="playButton">PLAY</button>
  </div>
  <div id="fps">FPS: 0</div>
  <div class="custom-cursor"></div>
  <!-- Debug info will be injected if enabled -->
  <script type="module">
    import * as THREE from "https://esm.sh/three@0.175.0";
    import { GUI } from "https://esm.sh/dat.gui@0.7.9";

    // ==== JS CODE START ====
    // (Paste the entire JavaScript you posted here, **but remove the first import lines**)
    // (Because imports are already provided above.)

    // START OF PASTE
    // ... (Paste all of your JavaScript, EXCLUDING the first two import lines)
    // ... (Everything else remains the same)
    // END OF PASTE

    // So, in your actual copy-paste, start at:
    // // Scene setup
    // let scene, camera, renderer;
    // ... etc ...

    // ==== JS CODE END ====
  </script>
</body>
</html>